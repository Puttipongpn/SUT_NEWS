<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css"
        integrity="sha384-xOolHFLEh07PJGoPkLv1IbcEPTNtaed2xpHsD9ESMhqIYd0nLMwNLD69Npy4HI+N" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"
        integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous">
        </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-Fy6S3B9q64WdZWQUiU+q4/2Lc9npb8tCaSX9FK7E8HnRr0Jz8D6OP9dO5Vg3Q9ct" crossorigin="anonymous"> //เลื่อนรูปภาพ
        </script>
    <link rel="stylesheet" href="/stylesheets/header.css">
    <!-- --------------------------------------------------------------------------------- -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;1,100;1,200&display=swap"
        rel="stylesheet" />

    <!-- AlpineJS -->
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Itim&family=Kanit:ital,wght@1,100&display=swap"
        rel="stylesheet">
    <title>CenterPage</title>
</head>
<style>
    /* CSS */
    .head-item:not(:last-child) {
        border-right: 1px solid #ccc;
        /* เพิ่มเส้นแบบแนวตั้งระหว่างปุ่มที่ไม่ใช่ปุ่มสุดท้าย */
        padding-right: 10px;
        /* ปรับระยะห่างของเส้นแบบแนวตั้ง */
    }

    /* เพิ่มคุณสมบัติ hover เมื่อเมาส์ชี้ไปยังปุ่ม */
    .head-item:not(:last-child):hover {
        background-color: #b34801;
        /* เปลี่ยนสีพื้นหลังเมื่อ hover */
    }

    .head-link {
        font-family: 'Itim', cursive;
    }

    .dropdown-item a {
        font-family: 'Itim', cursive;
    }

    #dropdownMenuButton {
        font-family: 'Itim', cursive;
    }

    .data-h {
        font-family: 'Itim', cursive;
    }
</style>

<body>
    <header>
        <div class="toppage d-flex justify-content-end">
            <ul class="head d-flex justify-content-center">
                <li class="head-item head-dropdown" style="padding-left: 5px;">
                    <a class="head-link" id="Asetting_page" href="/setting_page/setting_page_1">แก้ไขเว็ปเพจ</a>
                </li>
                <li class="head-item head-dropdown" style="padding-left: 8px;">
                    <a class="head-link" id="Arequest_ADMIN" href="/approve">รับคำร้องประกาศข่าว</a>
                </li>
                <li class="head-item head-dropdown" style="margin-left: 8px;">
                    <a class="head-link" id="Aallow_Offuser" href="/manage_users">จัดการคำร้องสมาชิกทางการ</a>
                </li>
                <li class="head-item ">
                    <a class="head-link" style="margin-left: 8px;" href="/Section" id="section_setting">เพิ่ม
                        section</a>
                </li>
                <li class="head-item ">
                    <a class="head-link" style="margin-left: 8px;" href="/setting_news"
                        id="setting_news">จัดการข่าวสาร</a>
                </li>
                <li class="head-item ">
                    <a class="head-link" style="margin-left: 8px;" id="subscriber" href="/subscriber">การติดตาม</a>
                </li>
                <li class="head-item " style="padding-left: 8px;">
                    <a class="head-link" id="bookmake" href="/bookmark">ข่าวที่บันทึกไว้</a>
                </li>
                <li class="head-item " style="margin-left: 8px;">
                    <a class="head-link" id="save_tags" href="/save_tags">แท็กที่บันทึกไว้</a>
                </li>

                <li class="head-item ">
                    <a style="margin-top: 3px;" type="button" id="dropdownMenuButton" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false" href="#">
                        Profile
                        <!-- Profile picture image-->
                        <% if (profile_image) { %>
                            <img id="profilePicture" class="img-account-profile rounded-circle " style="height: 30px;"
                                <% const imagePath=profile_image.substring(6); %>
                            src="<%= imagePath %>" alt="">
                                <% } else { %>
                                    <img id="profilePicture" class="img-account-profile rounded-circle mb-2"
                                        style="height: 30px;" src="http://bootdey.com/img/Content/avatar/avatar1.png"
                                        alt="">
                                    <% } %>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                        <div class="dropdown-item">
                            <a href="/profile">
                                <div class="profile-card-h">
                                    <div class="image-h">
                                        <% if (profile_image) { %>
                                            <img id="profilePicture" class="img-account-profile rounded-circle "
                                                style="height: 80px;" <% const imagePath=profile_image.substring(6); %>
                                            src="<%= imagePath %>" alt="">
                                                <% } else { %>
                                                    <img id="profilePicture" class="img-account-profile rounded-circle "
                                                        style="height: 80px;"
                                                        src="http://bootdey.com/img/Content/avatar/avatar1.png" alt="">
                                                    <% } %>
                                                        <!--<img src="images/profile.jpg" alt="" class="profile-pic">-->
                                    </div>
                                    <div class="data-h">
                                        <%= header.user_name %>
                                            <span>
                                                <%= header.role %>
                                            </span>
                                            <span>
                                                <%= header.email %>
                                            </span>
                                    </div>
                                </div>
                            </a>
                        </div>

                        <div class="dropdown-item">
                            <a href="/setting_profile" style="padding-left: 5px;" id="setting_profile">
                                <div style="padding-left: 5px;">
                                    setting profile
                                </div>
                            </a>
                        </div>
                        <div class="dropdown-item">
                            <a id="Logout" href="/logout" style="padding-left: 30px;">logout</a>
                        </div>
                        <div class="dropdown-item">
                            <a id="LOGIN" style="padding-left: 35px;" href="/logout">login</a>
                        </div>
                    </div>
                </li>
        </div>

        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var setting_page = document.getElementById('Asetting_page');
            if (currentUser.role === 'ADMIN') {
                setting_page.style.display = 'block'; // แสดงปุ่ม
            } else {
                setting_page.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var setting_page = document.getElementById('section_setting');
            if (currentUser.role === 'OFFICIAL USER' || currentUser.role === 'ADMIN') {
                setting_page.style.display = 'block'; // แสดงปุ่ม
            } else {
                setting_page.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var request_ADMIN = document.getElementById('Arequest_ADMIN');
            if (currentUser.role === 'ADMIN') {
                request_ADMIN.style.display = 'block'; // แสดงปุ่ม
            } else {
                request_ADMIN.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var allow_Offuser = document.getElementById('Aallow_Offuser');
            if (currentUser.role === 'ADMIN') {
                allow_Offuser.style.display = 'block'; // แสดงปุ่ม
            } else {
                allow_Offuser.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var allow_Offuser = document.getElementById('setting_profile');
            if (currentUser.role === 'ADMIN' || currentUser.role === 'USER' || currentUser.role === 'OFFICIAL USER') {
                allow_Offuser.style.display = 'block'; // แสดงปุ่ม
            } else {
                allow_Offuser.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var allow_Offuser = document.getElementById('Logout');
            if (currentUser.role === 'ADMIN' || currentUser.role === 'USER' || currentUser.role === 'OFFICIAL USER') {
                allow_Offuser.style.display = 'block'; // แสดงปุ่ม
            } else {
                allow_Offuser.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var userButton = document.getElementById('bookmake');
            if (currentUser.role === 'USER' || currentUser.role === 'OFFICIAL USER' || currentUser.role === 'ADMIN') {
                userButton.style.display = 'block'; // แสดงปุ่ม
            } else {
                userButton.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            console.log(currentUser.role)
            var userButton = document.getElementById('AddNEWs');
            if (currentUser.role === 'OFFICIAL USER' || currentUser.role === 'ADMIN') {
                userButton.style.display = 'block'; // แสดงปุ่ม
            } else {
                userButton.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var userButton = document.getElementById('setting_news');
            if (currentUser.role === 'OFFICIAL USER' || currentUser.role === 'ADMIN') {
                userButton.style.display = 'block'; // แสดงปุ่ม
            } else {
                userButton.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var userButton = document.getElementById('subscriber');
            if (currentUser.role === 'OFFICIAL USER' || currentUser.role === 'ADMIN' || currentUser.role === 'USER') {
                userButton.style.display = 'block'; // แสดงปุ่ม
            } else {
                userButton.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var userButton = document.getElementById('save_tags');
            if (currentUser.role === 'OFFICIAL USER' || currentUser.role === 'ADMIN' || currentUser.role === 'USER') {
                userButton.style.display = 'block'; // แสดงปุ่ม
            } else {
                userButton.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var userButton = document.getElementById('LOGIN');
            if (currentUser.role === 'GUEST') {
                userButton.style.display = 'block'; // แสดงปุ่ม
            } else {
                userButton.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        <script>
            var currentUser = {
                role: '<%= header.role %>' // ใส่ค่า row ที่ได้รับมาจากตัวแปร row ในหน้า HTML/EJS
            };
            var userButton = document.getElementById('LOGOUT');
            if (currentUser.role === 'USER' || currentUser.role === 'ADMIN' || currentUser.role === 'OFFICIAL USER') {
                userButton.style.display = 'block'; // แสดงปุ่ม
            } else {
                userButton.style.display = 'none'; // ซ่อนปุ่ม
            }
        </script>
        </ul>
        </div>
    </header>
</body>